/**
 * @file src/index.ts
 * @description Entry point for the stocks-interface Chalie tool.
 *
 * Bootstraps the daemon, reads the tool contract from stdin,
 * and delegates to the appropriate capability or sync handler.
 */

// TODO: implement daemon bootstrap
